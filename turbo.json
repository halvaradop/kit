{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": [".cache/.eslintcache"],
      "outputLogs": "new-only"
    },
    "//#lint:root": {
      "outputs": [".cache/.eslintcache"],
      "outputLogs": "new-only"
    },
    "lint:fix": {
      "dependsOn": ["^lint:fix"],
      "outputs": [".cache/.eslintcache"],
      "outputLogs": "new-only"
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  },
  "ui": "tui",
  "cacheDir": ".turbo/cache"
}
